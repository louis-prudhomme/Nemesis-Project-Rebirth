// this class only serves as a helper for some not game-related tasks
static class Helper {
  //returns the sign of a number
  static int getSign(float a) {
    if(a < 0) {return -1;}
    else if(a > 0) {return 1;}
    return 0;
  } 
}